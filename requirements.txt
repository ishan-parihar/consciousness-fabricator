# AI Meditation Engine - Dependencies

# TTS client (async HTTP)
httpx>=0.24.0

# Audio generation
numpy>=1.24.0

# Audio file I/O (optional - falls back to stdlib wave module)
soundfile>=0.12.0

# WebSocket API (optional - only needed for ws_api.py)
websockets>=12.0

# Config file loading (optional - falls back to JSON-only)
pyyaml>=6.0
